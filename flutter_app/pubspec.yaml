name: beacon_manager
description: BLE Beacon Manager for 30 MHz radio beacons — Sevskiy GmbH
publish_to: 'none'
version: 2.4.6+11

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_blue_plus: ^1.32.0
  permission_handler: ^11.0.0

  provider: ^6.1.0

  shared_preferences: ^2.2.0
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  path: ^1.9.0

  geolocator: ^11.0.0
  geocoding: ^3.0.0

  fl_chart: ^0.68.0

  intl: ^0.19.0
  share_plus: ^7.2.0
  audioplayers: ^6.1.0

  package_info_plus: ^8.0.0
  http: ^1.2.0
  open_filex: ^4.5.0

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
